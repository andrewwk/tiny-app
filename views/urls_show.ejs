<!DOCTYPE html>
<html lang="en">
<head>
  <title>Andrew's Tiny App</title>
  <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<body>
  <% include ./partials/_header.ejs %>
  <div class="container-message">
    <% if (message) { %>
      <strong><p><%= message %></p></strong>
      <% } %>
  </div>
  <div class="container-update" style="padding-top: 2em;">
    <div class="container-shorturl">
      <h4>Short URL: <%= shortURL %></h4>
    </div>
      <form method="post" action="/urls/<%= locals.shortURL %>?_method=PUT"style="display:block;">
        <input type="text" name="updated_url" style="width: 85%%; height: 1.75em;" placeholder="<%= users[user_id].urls[shortURL] %>">
        <button type="submit" name="button" style="display: inline-block" class="button">
          <strong>Update</strong>
        </button>
      </form>
    <form method="post" action="/urls/<%= url %>/delete?_method=DELETE" style="display:block; padding-top: 1em;">
      <button type="submit" name="button" class="fa fa-trash" aria-hidden="true">
        <strong>Delete</strong>
      </button>
    </form>
  </div>
</body>
</html>
