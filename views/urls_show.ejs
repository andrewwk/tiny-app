<!DOCTYPE html>
<html lang="en">
<head>
  <title>Andrew's Tiny App</title>
  <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<body>
  <% include ./partials/_header.ejs %>
  <div class="container-message">
    <% if (message) { %>
      <strong><p><%= message %></p></strong>
      <% } %>
  </div>
  <div class="container-update">
    <div class="container-shorturl">
      <h2>Short URL: <%= shortURL %></h2>
    </div>
    <div class="">
      <form method="post" action="/urls/<%= locals.shortURL %>"style="display:block;">
        <input type="text" name="updated_url" style="width: 80%" placeholder="<%= users[user_id].urls[shortURL] %>">
        <button type="submit" name="button" style="display: inline-block" class="button">
          <strong>Update</strong>
        </button>
      </form>
    </div>
    <form method="post" action="/urls/<%= url %>/delete" style="display:block;">
      <button type="submit" name="button" class="fa fa-trash" aria-hidden="true">
        <strong>Delete</strong>
      </button>
    </form>
  </div>
</body>
</html>
